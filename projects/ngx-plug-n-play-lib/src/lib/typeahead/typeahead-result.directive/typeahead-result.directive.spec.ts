import { TypeaheadResultDirective } from './typeahead-result.directive';

describe('TypeaheadResultDirective', () => {
	it('should create an instance', () => {
		const directive = new TypeaheadResultDirective();
		expect(directive).toBeTruthy();
	});
});
